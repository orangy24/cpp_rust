# Question list

Personal notes. Official syllabus: https://roadmap.sh/cpp

Status: `[ ]` open · `[x]` done

## Setup
- [ ] Install compiler, run a program that prints one line

## Functions / pointers (roadmap: Functions, Pointers and References)
- [ ] What does `void f(std::vector<int>)` vs `void f(std::vector<int> const&)` promise?
- [ ] When is `T&&` a forwarding reference, not an rvalue ref?

## Memory
- [ ] Write a type that owns a heap buffer and cannot be copied
- [ ] When would you use `shared_ptr` instead of `unique_ptr`?

## OOP
- [ ] Why does a polymorphic base need a virtual destructor?
- [ ] What is the vtable used for?

## STL / templates
- [ ] Implement `contains` for `vector<T>` with a template
- [ ] Why can `string_view` dangle?

## Concurrency
- [ ] What does the mutex protect in a queue `push`/`pop`?

## Scratch (add yours below)
- [ ]
